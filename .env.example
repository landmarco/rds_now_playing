RDS_LOGIN=yer_login_here
RDS_PASSWORD=yer_password_here
RDS_IP=yer_rds_ip_here
# Telnet port on the encoder. Optional — defaults to 23, the encoder's own
# configuration port. Set it to 5423 to go back to the old router mapping.
RDS_PORT=23
